import React from 'react'

// This will be a bit of a complicated component. We'll need to track the dropdown state, allowing a user to 
// select the playlist, create the media, and add it to our playlists

const Dropdown = () => {
  return (
    <div>

    </div>
  )
}

export default Dropdown;
