import gql from 'graphql-tag';

// We're going to need this query more than a few times in our app, so let's just export it to be used wherever. 

const CURRENT_USER = gql``;

export default CURRENT_USER;